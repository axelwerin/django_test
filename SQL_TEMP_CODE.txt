--SQL TEMP CODE

ALTER TABLE books ALTER subject_id DROP NOT NULL;

--ALTER TABLE books ADD CONSTRAINT hasSubject NOT NULL (subject_id);


-- ALTER TABLE table ADD CONSTRAINT contraint_namn FOREIGN KEY (main_table_attribute) REFERENCES other_table(other_table_attribute);

-- PRIMARY KEY title, year (måste läggas sist om den innehåller två attribut)